// a function can call itself — that is called recursion!
// factorial(5) = 5 * 4 * 3 * 2 * 1 = 120

factorial(n)
    if n <= 1
        return 1
    return n * factorial(n - 1)

out(factorial(5))
out(factorial(0))
